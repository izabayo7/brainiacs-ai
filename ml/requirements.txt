# ML environment for Brainiacs AI.
# Primary artifacts (knowledge tracing + grading) need only the core stack;
# torch is for the optional DKT comparison.

# --- core ---
pandas==2.2.3
numpy==2.1.3
scipy==1.17.1
scikit-learn==1.6.0
matplotlib==3.10.0
seaborn==0.13.2

# --- knowledge tracing (DKT) ---
torch==2.6.0            # optional: only the DKT cells need it; BKT is pure NumPy

# --- data download helper (ASSISTments lives on Google Drive) ---
gdown==5.2.0

# --- notebook execution ---
jupyter==1.1.1
nbformat==5.10.4
nbclient==0.10.2
ipykernel==6.29.5

# --- exploratory notebook only (ml/exploratory/) ---
datasets==3.2.0
xgboost==2.1.3
anthropic==0.42.0
